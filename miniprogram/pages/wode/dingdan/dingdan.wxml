<wxs module="m1">
  var changeName = function (name, index) {
    var newName = name + index
    return newName
  }
  module.exports = {
    changeName: changeName,
  }
</wxs>
<!-- 订单表头 -->
<view class="page__bd">
  <!-- <view class="weui-cells__title">新消息提示跟摘要信息后，统一在列表右侧</view> -->
  <view class="">
    <view class="ddTouBu border-botom">
      <view class="weui-cell__bd flex_1 {{isDaiqu ? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="tapDaiqu">代取</view>
      <view class="weui-cell__bd flex_1 {{isJijian ? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="tapJijian">寄件</view>
      <view class="weui-cell__bd flex_1 {{isPaotui ? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="tapPaotui">跑腿</view>
      <view class="weui-cell__bd flex_1 {{isMeishi ? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="tapMeishi">美食</view>
      <view class="weui-cell__bd flex_1 {{isQita ? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="tapQita">其他</view>
    </view>
  </view>
  <!-- 代取 订单选项 -->
  <view class="" wx:if="{{isDaiqu}}">
    <view class="padding-xs" style="">
      <view class="flex text-center">
        <view class="weui-cell__bd flex_1 {{isAll_daiqu ? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="toAll_daiqu">全部</view>
        <view class="weui-cell__bd flex_1 {{isChuli_daiqu ? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="toChuli_daiqu">待处理</view>
        <view class="weui-cell__bd flex_1 {{isQujianzhong_daiqu ? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="toQujianzhong">取件中</view>
        <view class="weui-cell__bd flex_1 {{isPaisong_daiqu? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="toPaisong">派送中</view>
        <view class="weui-cell__bd flex_1 {{isWancheng_daiqu? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="toWancheng">已送达</view>
        <view class="weui-cell__bd flex_1 {{isTuikuan_daiqu ? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="toTuikuan">退款</view>
      </view>
      <view class="flex">

        <view class="text-center flex_1" hover-class="hover" catchtap="toAll_daiqu">{{dingdanList.length}}</view>
        <view class="text-center flex_1" hover-class="hover" catchtap="toChuli_daiqu">{{dingdanList_dq_chuli.length}}</view>
        <view class="text-center flex_1" hover-class="hover" catchtap="toQujianzhong">{{dingdanList_dq_qujianzhong.length}}</view>
        <view class="text-center flex_1" hover-class="hover" catchtap="toPaisong">{{dingdanList_dq_paisong.length}}</view>
        <view class="text-center flex_1" hover-class="hover" catchtap="toWancheng">{{dingdanList_dq_wancheng.length}}</view>
        <view class="text-center flex_1" hover-class="hover" catchtap="toTuikuan">{{dingdanList_dq_tuikuan.length}}</view>
      </view>
    </view>

  </view>
  <!-- 寄件订单选项 -->
  <view class="" wx:if="{{isJijian}}">
    <view class="ddTouBu">
      <view class="weui-cell__bd flex_1 {{isAll_jijian ? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="toAll_jijian">全部</view>
      <view class="weui-cell__bd flex_1 {{isChuli ? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="toChuli">待处理</view>
      <view class="weui-cell__bd flex_1 {{isJichu ? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="toYijichu">已处理</view>
      <view class="weui-cell__bd flex_1 {{isQuxiao ? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="toQuxiao">已取消</view>
    </view>
  </view>
  <!-- 跑腿订单选项 -->
  <view class="" wx:if="{{isPaotui}}">
    <view class="ddTouBu">
      <view class="weui-cell__bd flex_1 {{isAll_paotui ? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="toAll_paotui">全部</view>
      <view class="weui-cell__bd flex_1 {{isChuli_paotui ? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="toChuli_paotui">待处理</view>
      <view class="weui-cell__bd flex_1 {{isWancheng_paotui? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="toWancheng_paotui">已完成</view>
      <view class="weui-cell__bd flex_1 {{isQuxiao_paotui ? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="toQuxiao_paotui">已取消</view>
    </view>
  </view>
  <!-- 美食订单选项 -->
  <view class="" wx:if="{{isMeishi}}">
    <view class="ddTouBu">
      <!-- <view class="weui-cell__bd flex_1 {{isAll ? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="toAll">
        全部</view>
      <view class="weui-cell__bd flex_1 {{isFukuan ? 'class_toTap' : 'class_unTap'}}" hover-class="hover"
        catchtap="tofukuan">待付款</view>
      <view class="weui-cell__bd flex_1 {{isQueren ? 'class_toTap' : 'class_unTap'}}" hover-class="hover"
        catchtap="toQueren">待确认</view>
      <view class="weui-cell__bd flex_1 {{isPingjia ? 'class_toTap' : 'class_unTap'}}" hover-class="hover"
        catchtap="toPingjia">待评价</view>
      <view class="weui-cell__bd flex_1 {{isTuikuan ? 'class_toTap' : 'class_unTap'}}" hover-class="hover"
        catchtap="toTuikuan">退款</view> -->

      <view class="weui-cell__bd flex_1 {{isAll_meishi ? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="toAll_meishi">全部</view>
      <view class="weui-cell__bd flex_1 {{isChuli_meishi ? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="toChuli_meishi">待处理</view>
      <view class="weui-cell__bd flex_1 {{isWancheng_meishi? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="toWancheng_meishi">已完成</view>
      <view class="weui-cell__bd flex_1 {{isTuikuan_meishi ? 'class_toTap' : 'class_unTap'}}" hover-class="hover" catchtap="toTuikuan_meishi">退款</view>
    </view>
  </view>
</view>
<!-- 搜索框 -->
<view class="weui-search-bar {{isInputShowed ? 'weui-search-bar_focusing' : ''}}" id="searchBar" wx:if="{{isAll_daiqu}}">
  <form class="weui-search-bar__form">
    <view class="weui-search-bar__box">
      <i class="weui-icon-search"></i>
      <input type="text" class="weui-search-bar__input" placeholder="搜索" value="{{inputVal}}" focus="{{isInputShowed}}" bindinput="inputTyping" bindconfirm="toSearch" confirm-type="search" />
      <span class="weui-icon-clear" wx:if="{{inputVal.length > 0}}" bindtap="clearInput"></span>
    </view>
    <label class="weui-search-bar__label" bindtap="showInput">
      <i class="weui-icon-search"></i>
      <span class="weui-search-bar__text">搜索</span>
    </label>
  </form>
  <view class="weui-search-bar__cancel-btn" bindtap="hideInput">取消</view>
</view>
<view wx:if="{{isInputShowed && isShowKong}}">
  <view style="text-align:center;padding:50rpx 0" wx:if="{{searchResultList.length == 0}}">
    <text>空空如也...</text>
  </view>
</view>

<!-- 取件中 提醒 -->
<view>
  <view wx:if="{{isQujianzhong_daiqu}}" style="background-color:gold; color:blue;" class="">
    <view class="" style="text-align: center;">

      {{text_quxiao1}}

    </view>
    <view style="text-align: center;">
      {{text_quxiao2}}

    </view>
  </view>
</view>

<view class="page__bd">
  <!-- 代取订单列表 -->
  <view class="" wx:if="{{isDaiqu}}">
    <!-- 全部订单列表 -->
    <view class="weui-form-preview" wx:if="{{isAll_daiqu}}" bindtap="toXiangQing" hover-class="hover" wx:for="{{isInputShowed ? searchResultList : dingdanList}}" wx:key="index" data-id="{{item._id}}" data-index="{{index}}">
      <view class="weui-form-preview__hd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">取货码</label>
          <em class="weui-form-preview__value">{{item.qh_Ma}}</em>
        </view>
      </view>
      <view class="weui-form-preview__bd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">快递点</label>
          <text class="weui-form-preview__value">{{item.kd_Dian}}</text>
        </view>
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">下单时间</label>
          <text class="weui-form-preview__value">{{item.xd_time}}</text>
        </view>
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">送达地点</label>
          <text class="weui-form-preview__value">{{item.sd_Didian}}</text>
        </view>
        <view class="weui-form-preview__item" wx:if="{{item.value_tkyy}}">
          <label class="weui-form-preview__label">退款原因</label>
          <text class="weui-form-preview__value" style="color: rgb(245, 131, 94)">{{item.value_tkyy}}</text>
        </view>
        <view class="anniu">
          <view style="display: flex;">
            <view class="anniu_item" wx:if="{{item.dd_Status == '4'}}">
              <text>退款中</text>
            </view>
            <view class="anniu_item" wx:if="{{item.dd_Status == '5'}}">
              <text>退款成功</text>
            </view>
            <view class="anniu_item" wx:if="{{item.dd_Status == '6'}}">
              <text>退款失败</text>
            </view>
          </view>
        </view>
        <view class="weui-form-preview__item">
          <text class="weui-form-preview__value" style="color: rgb(165, 165, 165);font-size: small;">点击进入查看订单详情>></text>
        </view>
        <!-- 操作标签 编辑 删除-->
        <!-- <view wx:if="{{item.xd_time > yesterdayTime ? true:false}}">
          <view wx:if="{{item.xd_time < shaixuanTime ? true:false}}">
            <view class="weui-form-preview__item flex" style="margin-top:10rpx;">
              <view class="flex_1" style="padding-right:150rpx;" catchtap="toEdit" data-id="{{item._id}}"
                data-qh_ma="{{item.qh_Ma}}" data-kd_dian="{{item.kd_Dian}}">
                <mp-icon icon="pencil" color="black" size="{{18}}"></mp-icon>
                编辑
              </view>
              <view class="flex_1" style="padding-right:150rpx;border-left:2rpx solid gray;" catchtap="toCancel"
                data-id="{{item._id}}">
                <mp-icon icon="close2" color="black" size="{{18}}"></mp-icon>
                取消
              </view>
            </view>
          </view>
        </view> -->
      </view>
    </view>
    <!-- 处理中订单列表 -->
    <view class="weui-form-preview" wx:if="{{isChuli_daiqu}}" bindtap="toXiangQing" hover-class="hover" wx:for="{{isInputShowed ? searchResultList : dingdanList_dq_chuli}}" wx:key="index" data-id="{{item._id}}" data-index="{{index}}">
      <view class="weui-form-preview__hd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">取货码</label>
          <em class="weui-form-preview__value">{{item.qh_Ma}}</em>
        </view>
      </view>
      <view class="weui-form-preview__bd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">快递点</label>
          <text class="weui-form-preview__value">{{item.kd_Dian}}</text>
        </view>
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">下单时间</label>
          <text class="weui-form-preview__value">{{item.xd_time}}</text>
        </view>
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">送达地点</label>
          <text class="weui-form-preview__value">{{item.sd_Didian}}</text>
        </view>
        <view class="weui-form-preview__item">
          <text class="weui-form-preview__value" style="color: rgb(165, 165, 165);font-size: small;">点击进入查看订单详情>></text>
        </view>
        <!-- <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">派送微信</label>
          <text class="weui-form-preview__value">{{item.wxPaisong}}</text>
        </view> -->
        <!-- 新操作标签 -->
        <!-- 未支付 -->
        <view class="anniu" wx:if="{{item.dd_Status == '1'}}">
          <view style="display: flex;">
            <view class="anniu_item" style="width: 270rpx;">
              <text>未支付，请重新下单</text>
            </view>
            <view class="anniu_item_1" catchtap="toCancel" data-id="{{item._id}}">
              <text>删除订单</text>
            </view>
          </view>
        </view>
        <!-- 已付款 -->
        <view class="anniu" wx:if="{{item.dd_Status == '0'}}">
          <view wx:if="{{item.xd_time > yesterdayTime ? true:false}}">
            <view wx:if="{{item.xd_time < shaixuanTime ? true:false}}" style="display: flex;">
              <view class="anniu_item_1" catchtap="toEdit" data-id="{{item._id}}" data-qh_ma="{{item.qh_Ma}}" data-kd_dian="{{item.kd_Dian}}" data-dingdanhao="{{item.dingdanhao}}">
                <text>编辑</text>
              </view>
            </view>
          </view>
          <view class="anniu_item" catchtap="toSongda" data-id="{{item._id}}">
            <text>确认送达</text>
          </view>
        </view>

        <!-- 操作标签 编辑 删除-->
        <!-- <view wx:if="{{item.xd_time > yesterdayTime ? true:false}}">
          <view wx:if="{{item.xd_time < shaixuanTime ? true:false}}">
            <view class="weui-form-preview__item flex" style="margin-top:10rpx;">
              <view class="flex_1" style="padding-right:150rpx;" catchtap="toEdit" data-id="{{item._id}}"
                data-qh_ma="{{item.qh_Ma}}" data-kd_dian="{{item.kd_Dian}}">
                <mp-icon icon="pencil" color="black" size="{{18}}"></mp-icon>
                编辑
              </view>
              <view class="flex_1" style="padding-right:150rpx;border-left:2rpx solid gray;" catchtap="toCancel" data-id="{{item._id}}">
                <mp-icon icon="close2" color="black" size="{{18}}"></mp-icon>
                取消
              </view>
            </view>
          </view>
        </view> -->
      </view>
    </view>
    <!-- 取件中 订单列表 -->
    <view class="weui-form-preview" wx:if="{{isQujianzhong_daiqu}}" bindtap="toXiangQing" hover-class="hover" wx:for="{{isInputShowed ? searchResultList : dingdanList_dq_qujianzhong}}" wx:key="index" data-id="{{item._id}}" data-index="{{index}}">
      <view class="weui-form-preview__hd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">取货码</label>
          <em class="weui-form-preview__value">{{item.qh_Ma}}</em>
        </view>
      </view>
      <view class="weui-form-preview__bd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">快递点</label>
          <text class="weui-form-preview__value">{{item.kd_Dian}}</text>
        </view>
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">下单时间</label>
          <text class="weui-form-preview__value">{{item.xd_time}}</text>
        </view>
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">送达地点</label>
          <text class="weui-form-preview__value">{{item.sd_Didian}}</text>
        </view>
        <view class="weui-form-preview__item">
          <text class="weui-form-preview__value" style="color: rgb(165, 165, 165);font-size: small;">点击进入查看订单详情>></text>
        </view>

        <view class="anniu" wx:if="{{item.dd_Status == '2'}}">
          <view wx:if="{{item.xd_time > yesterdayTime ? true:false}}">
            <!-- <view wx:if="{{item.xd_time < shaixuanTime ? true:false}}" style="display: flex;">
              <view class="anniu_item_1" catchtap="toEdit" data-id="{{item._id}}" data-qh_ma="{{item.qh_Ma}}" data-kd_dian="{{item.kd_Dian}}">
                <text>编辑</text>
              </view>
            </view> -->
          </view>
          <view class="anniu_item" catchtap="toSongda" data-id="{{item._id}}">
            <text>确认送达</text>
          </view>
        </view>
      </view>
    </view>

    <!-- 派送中 订单列表 -->
    <view class="weui-form-preview" wx:if="{{isPaisong_daiqu}}" bindtap="toXiangQing" hover-class="hover" wx:for="{{isInputShowed ? searchResultList : dingdanList_dq_paisong}}" wx:key="index" data-id="{{item._id}}" data-index="{{index}}">
      <view class="weui-form-preview__hd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">取货码</label>
          <em class="weui-form-preview__value">{{item.qh_Ma}}</em>
        </view>
      </view>
      <view class="weui-form-preview__bd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">快递点</label>
          <text class="weui-form-preview__value">{{item.kd_Dian}}</text>
        </view>
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">下单时间</label>
          <text class="weui-form-preview__value">{{item.xd_time}}</text>
        </view>
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">送达地点</label>
          <text class="weui-form-preview__value">{{item.sd_Didian}}</text>
        </view>
        <view class="weui-form-preview__item">
          <text class="weui-form-preview__value" style="color: rgb(165, 165, 165);font-size: small;">点击进入查看订单详情>></text>
        </view>

        <view class="anniu" wx:if="{{item.dd_Status == '7'|item.dd_Status == '8'}}">
          <view wx:if="{{item.xd_time > yesterdayTime ? true:false}}">
            <!-- <view wx:if="{{item.xd_time < shaixuanTime ? true:false}}" style="display: flex;">
              <view class="anniu_item_1" catchtap="toEdit" data-id="{{item._id}}" data-qh_ma="{{item.qh_Ma}}" data-kd_dian="{{item.kd_Dian}}">
                <text>编辑</text>
              </view>
            </view> -->
          </view>
          <view class="anniu_item" catchtap="toSongda" data-id="{{item._id}}">
            <text>确认送达</text>
          </view>
        </view>
      </view>
    </view>
    <!-- 完成 订单列表 -->
    <view class="weui-form-preview" wx:if="{{isWancheng_daiqu}}" bindtap="toXiangQing" hover-class="hover" wx:for="{{isInputShowed ? searchResultList : dingdanList_dq_wancheng}}" wx:key="index" data-id="{{item._id}}" data-index="{{index}}">
      <view class="weui-form-preview__hd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">取货码</label>
          <em class="weui-form-preview__value">{{item.qh_Ma}}</em>
        </view>
      </view>
      <view class="weui-form-preview__bd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">快递点</label>
          <text class="weui-form-preview__value">{{item.kd_Dian}}</text>
        </view>
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">下单时间</label>
          <text class="weui-form-preview__value">{{item.xd_time}}</text>
        </view>
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">送达地点</label>
          <text class="weui-form-preview__value">{{item.sd_Didian}}</text>
        </view>
        <view class="weui-form-preview__item">
          <text class="weui-form-preview__value" style="color: rgb(165, 165, 165);font-size: small;">点击进入查看订单详情>></text>
        </view>
      </view>

    </view>
    <!-- 退款 订单列表 -->
    <view class="weui-form-preview" wx:if="{{isTuikuan_daiqu}}" bindtap="toXiangQing" hover-class="hover" wx:for="{{isInputShowed ? searchResultList : dingdanList_dq_tuikuan}}" wx:key="index" data-id="{{item._id}}" data-index="{{index}}">
      <view class="weui-form-preview__hd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">取货码</label>
          <em class="weui-form-preview__value">{{item.qh_Ma}}</em>
        </view>
      </view>
      <view class="weui-form-preview__bd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">快递点</label>
          <text class="weui-form-preview__value">{{item.kd_Dian}}</text>
        </view>
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">下单时间</label>
          <text class="weui-form-preview__value">{{item.xd_time}}</text>
        </view>
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">送达地点</label>
          <text class="weui-form-preview__value">{{item.sd_Didian}}</text>
        </view>
        <view class="weui-form-preview__item" wx:if="{{item.value_tkyy}}">
          <label class="weui-form-preview__label">退款原因</label>
          <text class="weui-form-preview__value" style="color: rgb(245, 131, 94)">{{item.value_tkyy}}</text>
        </view>
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">订单号</label>
          <text style="color: rgb(156, 156, 156);" class="weui-form-preview__value">{{item.dingdanhao}}</text>
        </view>
        <view class="weui-form-preview__item">
          <text class="weui-form-preview__value" style="color: rgb(165, 165, 165);font-size: small;">点击进入查看订单详情>></text>
        </view>

        <view class="anniu">
          <view style="display: flex;">
            <view class="anniu_item" wx:if="{{item.dd_Status == '4'}}">
              <text>退款中</text>
            </view>
            <view class="anniu_item" wx:if="{{item.dd_Status == '5'}}">
              <text>退款成功</text>
            </view>
            <view class="anniu_item" wx:if="{{item.dd_Status == '6'}}">
              <text>退款失败</text>
            </view>
          </view>
        </view>
      </view>
    </view>
  </view>
  <!-- 寄件订单列表 -->
  <view class="" wx:if="{{isJijian}}">
    <!-- 全部订单列表 -->
    <view class="weui-form-preview" wx:if="{{isAll_jijian}}" bindtap="toXiangQing" hover-class="hover" wx:for="{{isInputShowed ? searchResultList : dingdanList}}" wx:key="index" data-id="{{item._id}}" data-index="{{index}}">
      <view class="weui-form-preview__hd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">目的地</label>
          <em class="weui-form-preview__value" style="font-size:25rpx;">{{item.mdd}}</em>
        </view>
      </view>
      <view class="weui-form-preview__bd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">重量</label>
          <text class="weui-form-preview__value">{{item.JJ_weight}}</text>
        </view>
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">下单时间</label>
          <text class="weui-form-preview__value">{{item.xd_time}}</text>
        </view>
        <view class="weui-form-preview__item">
          <text class="weui-form-preview__value" style="color: rgb(165, 165, 165);font-size: small;">点击进入查看订单详情>></text>
        </view>
        <!-- 操作标签 编辑 删除-->
        <!-- <view wx:if="{{item.xd_time > yesterdayTime ? true:false}}"
          style="border-top:2rpx solid #f0f0f0;margin-top:20rpx;">
          <view wx:if="{{item.xd_time < shaixuanTime ? true:false}}">
            <view wx:if="{{item.dd_Status < '3'}}">
              <view class="weui-form-preview__item flex" style="margin-top:10rpx;">
                <view class="flex_1" style="padding-right:150rpx;" catchtap="toEdit" data-id="{{item._id}}"
                  data-qh_ma="{{item.qh_Ma}}" data-kd_dian="{{item.kd_Dian}}">
                  <mp-icon icon="pencil" color="black" size="{{16}}"></mp-icon>
                  编辑
                </view>
                <view class="flex_1" style="padding-right:150rpx;border-left:2rpx solid gray;" catchtap="toCancel"
                  data-id="{{item._id}}">
                  <mp-icon icon="close2" color="black" size="{{16}}"></mp-icon>
                  取消
                </view>
              </view>
            </view>
          </view>
        </view> -->
      </view>
    </view>
    <!-- 处理中订单列表 -->
    <view class="weui-form-preview" wx:if="{{isChuli}}" bindtap="toXiangQing" hover-class="hover" wx:for="{{isInputShowed ? searchResultList : dingdanListJJchuli}}" wx:key="index" data-id="{{item._id}}" data-index="{{index}}">

      <view class="weui-form-preview__hd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">目的地</label>
          <em class="weui-form-preview__value" style="font-size:25rpx;">{{item.mdd}}</em>
        </view>
      </view>
      <view class="weui-form-preview__bd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">重量</label>
          <text class="weui-form-preview__value">{{item.JJ_weight}}</text>
        </view>
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">下单时间</label>
          <text class="weui-form-preview__value">{{item.xd_time}}</text>
        </view>
        <view class="weui-form-preview__item">
          <text class="weui-form-preview__value" style="color: rgb(165, 165, 165);font-size: small;">点击进入查看订单详情>></text>
        </view>

        <view class="anniu">
          <view wx:if="{{item.xd_time > yesterdayTime ? true:false}}">
            <view wx:if="{{item.xd_time < shaixuanTime ? true:false}}" style="display: flex;">
              <view class="anniu_item_1" catchtap="toCancel" data-id="{{item._id}}">
                <text>取消</text>
              </view>
              <view class="anniu_item_1" catchtap="toEdit" data-id="{{item._id}}" data-qh_ma="{{item.qh_Ma}}" data-kd_dian="{{item.kd_Dian}}">
                <text>编辑</text>
              </view>
            </view>
          </view>
        </view>


        <!-- 操作标签 编辑 删除-->
        <!-- <view wx:if="{{item.xd_time > yesterdayTime ? true:false}}" style="border-top:2rpx solid #f0f0f0;">
          <view wx:if="{{item.xd_time < shaixuanTime ? true:false}}">
            <view class="weui-form-preview__item flex" style="margin-top:10rpx;">
              <view class="flex_1" style="padding-right:150rpx;" catchtap="toEdit" data-id="{{item._id}}"
                data-qh_ma="{{item.qh_Ma}}" data-kd_dian="{{item.kd_Dian}}">
                <mp-icon icon="pencil" color="black" size="{{16}}"></mp-icon>
                编辑
              </view>
              <view class="flex_1" style="padding-right:150rpx;border-left:2rpx solid gray;" catchtap="toCancel"
                data-id="{{item._id}}">
                <mp-icon icon="close2" color="black" size="{{16}}"></mp-icon>
                取消
              </view>
            </view>
          </view>
        </view> -->
      </view>
    </view>
    <!-- 已取消订单列表 -->
    <view class="weui-form-preview" wx:if="{{isQuxiao}}" bindtap="toXiangQing" hover-class="hover" wx:for="{{isInputShowed ? searchResultList : dingdanListJJquxiao}}" wx:key="index" data-id="{{item._id}}" data-index="{{index}}">
      <view class="weui-form-preview__hd">
        <view class="quxiao" wx:if="{{isQuxiao}}">
          <view class="quxiao_item">已取消</view>
        </view>
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">目的地</label>
          <em class="weui-form-preview__value" style="font-size:25rpx;">{{item.mdd}}</em>
        </view>
      </view>
      <view class="weui-form-preview__bd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">重量</label>
          <text class="weui-form-preview__value">{{item.JJ_weight}}</text>
        </view>
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">下单时间</label>
          <text class="weui-form-preview__value">{{item.xd_time}}</text>
        </view>
        <view class="weui-form-preview__item">
          <text class="weui-form-preview__value" style="color: rgb(165, 165, 165);font-size: small;">点击进入查看订单详情>></text>
        </view>
        <!-- 操作标签 编辑 删除-->
        <!-- <view wx:if="{{item.xd_time > yesterdayTime ? true:false}}">
          <view wx:if="{{item.xd_time < shaixuanTime ? true:false}}">
            <view class="weui-form-preview__item flex" style="margin-top:10rpx;">
              <view class="flex_1" style="padding-right:300rpx;padding-top:15rpx;border-top:2rpx solid #f0f0f0;"
                catchtap="toCancel" data-id="{{item._id}}">
                <mp-icon icon="close2" color="black" size="{{20}}"></mp-icon>
              </view>
            </view>
          </view>
        </view> -->
      </view>
    </view>
    <!-- 已寄出 订单列表 -->
    <view class="weui-form-preview" wx:if="{{isJichu}}" bindtap="toXiangQing" hover-class="hover" wx:for="{{isInputShowed ? searchResultList : dingdanListJJyijichu}}" wx:key="index" data-id="{{item._id}}" data-index="{{index}}">
      <view class="weui-form-preview__hd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">目的地</label>
          <em class="weui-form-preview__value" style="font-size:25rpx;">{{item.mdd}}</em>
        </view>
      </view>
      <view class="weui-form-preview__bd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">快递公司</label>
          <text class="weui-form-preview__value">{{item.company}}</text>
        </view>
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">运单号</label>
          <text class="weui-form-preview__value">{{item.yundanNum}}</text>
        </view>
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">重量</label>
          <text class="weui-form-preview__value">{{item.JJ_weight}}</text>
        </view>
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">下单时间</label>
          <text class="weui-form-preview__value">{{item.xd_time}}</text>
        </view>
        <view class="weui-form-preview__item">
          <text class="weui-form-preview__value" style="color: rgb(165, 165, 165);font-size: small;">点击进入查看订单详情>></text>
        </view>
      </view>
    </view>
  </view>
  <!-- 美食订单列表 -->
  <view class="" wx:if="{{isMeishi}}">
    <!-- 全部订单列表 -->
    <view class="weui-panel weui-panel_access" wx:if="{{isAll_meishi}}" catchtap="toXiangQing" hover-class="hover" wx:for="{{isInputShowed ? searchResultList : meishiList}}" wx:key="index" data-id="{{item._id}}" data-index="{{index}}">
      <view class="weui-panel__hd" style="display:flex;justify-content: space-between;align-items: center;">
        <view style="display: flex;align-items: center;">
          <image style="width: 40rpx;height: 40rpx;margin-right: 10rpx;" src="{{item.dianpu.dianpu_zhaopaiUrl.imagefileID}}"></image>
          {{item.dianpu.dianpu_name}}
        </view>
        <view class="weui-media-box__desc" style="margin-left:15rpx;font-weight: normal;">{{item.name}}
          {{item.describe}}
        </view>
      </view>
      <view class="weui-panel__bd">
        <a class="weui-media-box weui-media-box_appmsg">
          <view class="weui-media-box__hd">
            <image class="weui-media-box__thumb" src="{{item.describeImageurl.imagetempFileURL}}" alt></image>
          </view>
          <view class="weui-media-box__bd">
            <h4 class="weui-media-box__title" style="color:red;">￥ {{item.shijiMoney}}</h4>
            <view class="weui-media-box__desc">{{item.xd_time}}</view>
            <view class="dingdan_items">
              <view class="" style="color: gray;">由 商家 提供服务</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='0'}}">待出餐</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='3'}}">已送达</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='2'}}">配送中</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='4'}}">退款中</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='5'}}">退款成功</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='6'}}">退款失败</view>
              <view class="dingdan_item2" style="color: gray;" wx:if="{{item.dd_Status=='7'}}">已评价</view>
            </view>

          </view>
        </a>
        <a class="weui-media-box weui-media-box_appmsg" style="justify-content: flex-end;">
          <view class="anniu2" wx:if="{{item.dd_Status=='0'||item.dd_Status=='2'}}">
            <view class="anniu_item2" style="width: 150rpx;" catchtap="goSongda_meishi" hover-class="hover2" data-showlist_danxuan="{{item}}">确认送达</view>
          </view>

          <view class="anniu2" wx:if="{{item.dd_Status=='3'}}">
            <view class="anniu_item2" catchtap="goPingjia" hover-class="hover2" data-id="{{item._id}}" data-dianpu_name="{{item.dianpu.dianpu_name}}">评价</view>
          </view>
        </a>
      </view>
    </view>
    <!-- 处理中订单列表 -->
    <view class="weui-panel weui-panel_access" wx:if="{{isChuli_meishi}}" catchtap="toXiangQing" hover-class="hover" wx:for="{{isInputShowed ? searchResultList : dingdanList_ms_chuli}}" wx:key="index" data-id="{{item._id}}" data-index="{{index}}">
      <view class="weui-panel__hd" style="display:flex;justify-content: space-between;align-items: center;">
        <view style="display: flex;align-items: center;">
          <image style="width: 40rpx;height: 40rpx;margin-right: 10rpx;" src="{{item.dianpu.dianpu_zhaopaiUrl.imagefileID}}"></image>
          {{item.dianpu.dianpu_name}}
        </view>
        <view class="weui-media-box__desc" style="margin-left:15rpx;font-weight: normal;">{{item.name}}
          {{item.describe}}
        </view>
      </view>
      <view class="weui-panel__bd">
        <a class="weui-media-box weui-media-box_appmsg">
          <view class="weui-media-box__hd">
            <image class="weui-media-box__thumb" src="{{item.describeImageurl.imagetempFileURL}}" alt></image>
          </view>
          <view class="weui-media-box__bd">
            <h4 class="weui-media-box__title" style="color:red;">￥ {{item.shijiMoney}}</h4>
            <view class="weui-media-box__desc">{{item.xd_time}}</view>
            <view class="dingdan_items">
              <view class="" style="color: gray;">由 商家 提供服务</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='0'}}">待出餐</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='3'}}">已送达</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='2'}}">配送中</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='4'}}">退款中</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='5'}}">退款成功</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='6'}}">退款失败</view>
            </view>
          </view>
        </a>
        <a class="weui-media-box weui-media-box_appmsg" style="justify-content: flex-end;">
          <view class="anniu2" wx:if="{{item.dd_Status=='0'||item.dd_Status=='2'}}">
            <view class="anniu_item2" style="width: 160rpx;" catchtap="goSongda_meishi" hover-class="hover2" data-showlist_danxuan="{{item}}">确认送达</view>
          </view>
        </a>
      </view>
    </view>
    <!-- 完成 订单列表 -->
    <view class="weui-panel weui-panel_access" wx:if="{{isWancheng_meishi}}" catchtap="toXiangQing" hover-class="hover" wx:for="{{isInputShowed ? searchResultList : dingdanList_ms_wancheng}}" wx:key="index" data-id="{{item._id}}" data-index="{{index}}">
      <view class="weui-panel__hd" style="display:flex;justify-content: space-between;align-items: center;">
        <view style="display: flex;align-items: center;">
          <image style="width: 40rpx;height: 40rpx;margin-right: 10rpx;" src="{{item.dianpu.dianpu_zhaopaiUrl.imagefileID}}"></image>
          {{item.dianpu.dianpu_name}}
        </view>
        <view class="weui-media-box__desc" style="margin-left:15rpx;font-weight: normal;">{{item.name}}
          {{item.describe}}
        </view>
      </view>
      <view class="weui-panel__bd">
        <a class="weui-media-box weui-media-box_appmsg">
          <view class="weui-media-box__hd">
            <image class="weui-media-box__thumb" src="{{item.describeImageurl.imagetempFileURL}}" alt></image>
          </view>
          <view class="weui-media-box__bd">
            <h4 class="weui-media-box__title" style="color:red;">￥ {{item.shijiMoney}}</h4>
            <view class="weui-media-box__desc">{{item.xd_time}}</view>
            <view class="dingdan_items">
              <view class="" style="color: gray;">由 商家 提供服务</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='0'}}">待出餐</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='3'}}">已送达</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='2'}}">配送中</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='4'}}">退款中</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='5'}}">退款成功</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='6'}}">退款失败</view>
              <view class="dingdan_item2" style="color: gray;" wx:if="{{item.dd_Status=='7'}}">已评价</view>

            </view>
          </view>
        </a>
        <a class="weui-media-box weui-media-box_appmsg" style="justify-content: flex-end;">
          <view class="anniu2" wx:if="{{item.dd_Status=='3'}}">
            <view class="anniu_item2" catchtap="goPingjia" hover-class="hover2" data-id="{{item._id}}" data-dianpu_name="{{item.dianpu.dianpu_name}}">评价</view>
          </view>
        </a>
      </view>
    </view>
    <!-- 退款 订单列表 -->
    <view class="weui-panel weui-panel_access" wx:if="{{isTuikuan_meishi}}" catchtap="toXiangQing" hover-class="hover" wx:for="{{isInputShowed ? searchResultList : dingdanList_ms_tuikuan}}" wx:key="index" data-id="{{item._id}}" data-index="{{index}}">
      <view class="weui-panel__hd" style="display:flex;justify-content: space-between;align-items: center;">
        <view style="display: flex;align-items: center;">
          <image style="width: 40rpx;height: 40rpx;margin-right: 10rpx;" src="{{item.dianpu.dianpu_zhaopaiUrl.imagefileID}}"></image>
          {{item.dianpu.dianpu_name}}
        </view>
        <view class="weui-media-box__desc" style="margin-left:15rpx;font-weight: normal;">{{item.name}}
          {{item.describe}}
        </view>
      </view>
      <view class="weui-panel__bd">
        <a class="weui-media-box weui-media-box_appmsg">
          <view class="weui-media-box__hd">
            <image class="weui-media-box__thumb" src="{{item.describeImageurl.imagetempFileURL}}" alt></image>
          </view>
          <view class="weui-media-box__bd">
            <h4 class="weui-media-box__title" style="color:red;">￥ {{item.shijiMoney}}</h4>
            <view class="weui-media-box__desc">{{item.xd_time}}</view>
            <view class="dingdan_items">
              <view class="" style="color: gray;">由 商家 提供服务</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='0'}}">待出餐</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='3'}}">已送达</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='2'}}">配送中</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='4'}}">退款中</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='5'}}">退款成功</view>
              <view class="dingdan_item2" style="color: #F5835E;" wx:if="{{item.dd_Status=='6'}}">退款失败</view>
            </view>
          </view>
        </a>
      </view>
    </view>

  </view>

  <!-- 跑腿 订单列表 -->
  <view class="" wx:if="{{isPaotui}}">
    <!-- 全部订单列表 -->
    <view class="weui-form-preview" wx:if="{{isAll_paotui}}" bindtap="toXiangQing" hover-class="hover" wx:for="{{isInputShowed ? searchResultList : paotuiList}}" wx:key="index" data-id="{{item._id}}" data-index="{{index}}">
      <view class="weui-form-preview__hd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">需求信息</label>
          <em class="weui-form-preview__value" style="font-size:25rpx;">{{item.xuqiu}}</em>
        </view>
      </view>
      <view class="weui-form-preview__bd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">下单时间</label>
          <text class="weui-form-preview__value">{{item.xd_time}}</text>
        </view>
        <view class="weui-form-preview__item">
          <text class="weui-form-preview__value" style="color: rgb(165, 165, 165);font-size: small;">点击进入查看订单详情>></text>
        </view>
      </view>
    </view>
    <!-- 待处理  -->
    <view class="weui-form-preview" wx:if="{{isChuli_paotui}}" bindtap="toXiangQing" hover-class="hover" wx:for="{{isInputShowed ? searchResultList : dingdanList_pt_chuli}}" wx:key="index" data-id="{{item._id}}" data-index="{{index}}">
      <view class="weui-form-preview__hd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">需求信息</label>
          <em class="weui-form-preview__value" style="font-size:25rpx;">{{item.xuqiu}}</em>
        </view>
      </view>
      <view class="weui-form-preview__bd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">下单时间</label>
          <text class="weui-form-preview__value">{{item.xd_time}}</text>
        </view>
        <view class="weui-form-preview__item">
          <text class="weui-form-preview__value" style="color: rgb(165, 165, 165);font-size: small;">点击进入查看订单详情>></text>
        </view>
      </view>
    </view>
    <!-- 完成  -->
    <view class="weui-form-preview" wx:if="{{isWancheng_paotui}}" bindtap="toXiangQing" hover-class="hover" wx:for="{{isInputShowed ? searchResultList : dingdanList_pt_wancheng}}" wx:key="index" data-id="{{item._id}}" data-index="{{index}}">
      <view class="weui-form-preview__hd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">需求信息</label>
          <em class="weui-form-preview__value" style="font-size:25rpx;">{{item.xuqiu}}</em>
        </view>
      </view>
      <view class="weui-form-preview__bd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">下单时间</label>
          <text class="weui-form-preview__value">{{item.xd_time}}</text>
        </view>
        <view class="weui-form-preview__item">
          <text class="weui-form-preview__value" style="color: rgb(165, 165, 165);font-size: small;">点击进入查看订单详情>></text>
        </view>
      </view>
    </view>
    <!-- 取消  -->
    <view class="weui-form-preview" wx:if="{{isQuxiao_paotui}}" bindtap="toXiangQing" hover-class="hover" wx:for="{{isInputShowed ? searchResultList : dingdanList_pt_quxiao}}" wx:key="index" data-id="{{item._id}}" data-index="{{index}}">
      <view class="weui-form-preview__hd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">需求信息</label>
          <em class="weui-form-preview__value" style="font-size:25rpx;">{{item.xuqiu}}</em>
        </view>
      </view>
      <view class="weui-form-preview__bd">
        <view class="weui-form-preview__item">
          <label class="weui-form-preview__label">下单时间</label>
          <text class="weui-form-preview__value">{{item.xd_time}}</text>
        </view>
        <view class="weui-form-preview__item">
          <text class="weui-form-preview__value" style="color: rgb(165, 165, 165);font-size: small;">点击进入查看订单详情>></text>
        </view>
      </view>
    </view>

  </view>

  <!-- 其他 订单列表 -->
  <view wx:if="{{isQita}}">
    <view class="tuikuan_items" wx:if="{{searchResultList.length == 0 && isInputShowed == false}}">
      <view class="dingdan" wx:for="{{qitaList}}" wx:key="zhu_index" wx:for-index="zhu_index" wx:for-item="zhu_item">
        <view class="dingdan_tuikuan" style="width: 100%;">
          <!-- 主题 -->
          <view class="dingdan_items">
            <view class="dingdan_item2 jiacu">{{zhu_item[qitaResdata[zhu_index].input_list.length+qitaResdata[zhu_index].choose_list.length+8]}}</view>
          </view>
          <!-- 输入框 -->
          <view wx:for="{{qitaResdata[zhu_index].input_list}}" wx:key="index_input" wx:for-index="index_input" wx:for-item="item_input">
            <view class="dingdan_items">
              <view class="dingdan_item1">{{item_input.input_title}}</view>
              <view class="dingdan_item2 ">{{zhu_item[index_input+5]}}</view>
            </view>
          </view>
          <!-- 选择框 -->
          <view wx:for="{{qitaResdata[zhu_index].choose_list}}" wx:key="index_choose" wx:for-index="index_choose" wx:for-item="item_choose">
            <view class="dingdan_items">
              <view class="dingdan_item1">{{item_choose.choose_title}}</view>
              <view class="dingdan_item2 ">{{zhu_item[qitaResdata[zhu_index].input_list.length+index_choose+6]}}</view>
            </view>
          </view>
          <!-- 支付选项 -->
          <view wx:if="{{qitaResdata[zhu_index].zhifu.isOpen_zhifu}}">
            <view class="dingdan_items">
              <view class="dingdan_item1">{{qitaResdata[zhu_index].zhifu.zhifu_title}}</view>
              <view class="dingdan_item2 ">{{zhu_item[qitaResdata[zhu_index].input_list.length+qitaResdata[zhu_index].choose_list.length+6]}}</view>
            </view>
            <view class="dingdan_items">
              <view class="dingdan_item1 jiacu">金额</view>
              <view class="dingdan_item2 jiacu">¥{{zhu_item[2]}}</view>
            </view>
          </view>
          <!-- 其他信息 -->
          <view class="dingdan_items">
            <view class="dingdan_item1">订单编号</view>
            <view class="dingdan_item2 ">{{zhu_item[4]}}</view>
          </view>
          <view class="dingdan_items">
            <view class="dingdan_item1">下单时间</view>
            <view class="dingdan_item2 ">{{zhu_item[qitaResdata[zhu_index].input_list.length+qitaResdata[zhu_index].choose_list.length+10]}}</view>
          </view>


          <!-- <view class="dingdan_items">
            <view class="dingdan_item1">申请退款</view>
            <view class="dingdan_item2">{{item.tk_time}}</view>
          </view>
          <view class="dingdan_items" style="color: #F5835E;" wx:if="{{item.isJutui}}">
            <view>拒绝退款</view>
            <view>{{item.jt_time?item.jt_time:item.tk_time}}</view>
          </view>
          <view class="dingdan_items" style="color: #F5835E;" wx:if="{{!item.isJutui}}">
            <view>退款完成</view>
            <view>{{item.tytuikuan_time?item.tytuikuan_time:item.tk_time}}</view>
          </view> -->

        </view>
      </view>
    </view>
    <view class="tuikuan_items" wx:if="{{searchResultList.length !== 0 && isInputShowed == true}}">
      <view class="dingdan" wx:for="{{searchResultList}}" wx:key="zhu_index" wx:for-index="zhu_index" wx:for-item="zhu_item">
        <view class="dingdan_tuikuan" style="width: 100%;">
          <!-- 主题 -->
          <view class="dingdan_items">
            <view class="dingdan_item2 jiacu">{{zhu_item[qitaResdata_search[zhu_index].input_list.length+qitaResdata_search[zhu_index].choose_list.length+8]}}</view>
          </view>
          <!-- 输入框 -->
          <view wx:for="{{qitaResdata_search[zhu_index].input_list}}" wx:key="index_input" wx:for-index="index_input" wx:for-item="item_input">
            <view class="dingdan_items">
              <view class="dingdan_item1">{{item_input.input_title}}</view>
              <view class="dingdan_item2 ">{{zhu_item[index_input+5]}}</view>
            </view>
          </view>
          <!-- 选择框 -->
          <view wx:for="{{qitaResdata_search[zhu_index].choose_list}}" wx:key="index_choose" wx:for-index="index_choose" wx:for-item="item_choose">
            <view class="dingdan_items">
              <view class="dingdan_item1">{{item_choose.choose_title}}</view>
              <view class="dingdan_item2 ">{{zhu_item[qitaResdata_search[zhu_index].input_list.length+index_choose+6]}}</view>
            </view>
          </view>
          <!-- 支付选项 -->
          <view wx:if="{{qitaResdata_search[zhu_index].zhifu.isOpen_zhifu}}">
            <view class="dingdan_items">
              <view class="dingdan_item1">{{qitaResdata_search[zhu_index].zhifu.zhifu_title}}</view>
              <view class="dingdan_item2 ">{{zhu_item[qitaResdata_search[zhu_index].input_list.length+qitaResdata_search[zhu_index].choose_list.length+6]}}</view>
            </view>
            <view class="dingdan_items">
              <view class="dingdan_item1 jiacu">金额</view>
              <view class="dingdan_item2 jiacu">¥{{zhu_item[2]}}</view>
            </view>
          </view>
          <!-- 其他信息 -->
          <view class="dingdan_items">
            <view class="dingdan_item1">订单编号</view>
            <view class="dingdan_item2 ">{{zhu_item[4]}}</view>
          </view>
          <view class="dingdan_items">
            <view class="dingdan_item1">下单时间</view>
            <view class="dingdan_item2 ">{{zhu_item[qitaResdata_search[zhu_index].input_list.length+qitaResdata_search[zhu_index].choose_list.length+10]}}</view>
          </view>


          <!-- <view class="dingdan_items">
            <view class="dingdan_item1">申请退款</view>
            <view class="dingdan_item2">{{item.tk_time}}</view>
          </view>
          <view class="dingdan_items" style="color: #F5835E;" wx:if="{{item.isJutui}}">
            <view>拒绝退款</view>
            <view>{{item.jt_time?item.jt_time:item.tk_time}}</view>
          </view>
          <view class="dingdan_items" style="color: #F5835E;" wx:if="{{!item.isJutui}}">
            <view>退款完成</view>
            <view>{{item.tytuikuan_time?item.tytuikuan_time:item.tk_time}}</view>
          </view> -->

        </view>
      </view>
    </view>
  </view>

  <!-- 底部加载 无更多 显示 -->
  <view class="weui-loadmore" wx:if="{{isShowLoading}}">
    <i class="weui-loading"></i>
    <text class="weui-loadmore__tips">正在加载</text>
  </view>
  <view class="weui-loadmore weui-loadmore_line" wx:if="{{isZanWu}}">
    <text class="weui-loadmore__tips">暂无数据</text>
  </view>
  <view class="weui-loadmore weui-loadmore_line weui-loadmore_dot" wx:if="{{isGengDuo}}">
  </view>
</view>
<view class="fadeIn" wx:if="{{isCancel}}">
  <view class="weui-mask" bindtap="toCancelclose"></view>
  <view class="weui-dialog">
    <view class="weui-dialog__hd"><strong class="weui-dialog__title">提示</strong></view>
    <view class="weui-dialog__bd">取消订单？</view>
    <view class="weui-dialog__ft">
      <a class="weui-dialog__btn weui-dialog__btn_default" bindtap="toCancelclose">取消</a>
      <a class="weui-dialog__btn weui-dialog__btn_primary" bindtap="toCancelconfirm">确定</a>
    </view>
  </view>
</view>